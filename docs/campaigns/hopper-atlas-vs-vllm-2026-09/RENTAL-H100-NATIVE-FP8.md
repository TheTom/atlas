# Original FP8 checkpoint follow-through

Snapshot through 2026-09-06 00:54 UTC on the same single H100. These sessions read `Qwen/Qwen3.8-27B-FP8` revision `017b9c7af6b5689d5dd426a76e0bc077eb5ca20a` from the retained shared snapshot. Butter now loads that original format and passes its numerical and protocol checks under the sole reversal exception. Atlas's first native-FP8 run is interrupted at C16; a separate repaired run now completes both latency rungs. Native C16 reaches 64.998 tokens/s against vLLM's 790.507 under the recorded profiles. None is a certified full campaign result.

## First Atlas native profile: completed C1, interrupted C16

Source `b3025ab19d30416a3bbef201f34989bf338e1acd`, binary SHA256 `e4b7468d6f35ce6dde1251142b23eb528fc23df79950b41d33fcef54fc290494`, runs from a canonical `spark` basename. The owned process records `ATLAS_DENSE_FP8=1` and `--lm-head-dtype bf16`. The real serve log contains 48 native-FP8 GDN load records and `LM head kept as BF16 (skip NVFP4 quantization per model config)`. This is a distinct profile from the earlier default Atlas C1 result at 51.208 tokens/s.

The M4–8 FFN dispatch repair now chooses the original FP8 overlay instead of its NVFP4 fallback. Its production dispatch oracle first observed zero of three required native projections at M4 (exit101); after repair, 647 model CPU tests pass, 14 remain ignored, and formatted focused checks/rustdoc/clippy pass. The CPU test proves route selection, while loading messages and the executed profile establish the subsequent runtime context. They do not substitute for a comprehensive GPU kernel trace. [Original precision audit and limitations](evidence/rental-h100-20260905/native-fp8-followup/atlas-native-route-cpu/PRECISION-AUDIT.md).

| Native Atlas `lat01` | C1 | C16 |
|---|---:|---|
| Completed recorded repetitions | 3 | No completed rung |
| Output tokens/s, mean of recorded repetitions | 28.925658 | Not reported |
| TTFT p50/p99, mean of per-repetition values | 3,409.423 ms / 3,409.423 ms | Not reported |
| TPOT p50, mean of per-repetition values | 21.333265 ms | Not reported |
| Actual prompt/output tokens per completed request | 1,193 / 256 | Not established by a completed rung |
| Recorded errors in completed rung | 0 | Interrupted; not a zero-error claim |

The frozen ladder remains unmodified: nominal ISL1,024 / OSL256, one warmup, three repetitions, C1/C16, seed42, temperature0, think/spec off. C1's per-repetition p50 and p99 each contain one observation; these means are not pooled percentiles. [Unchanged ladder JSON](evidence/rental-h100-20260905/native-fp8-followup/atlas-native-lat01/ladder.json).

Boot passes at 23.435 s to health plus 0.344 s for its first token. Pre-coherency passes determinism, typed tools, think-off, arithmetic391 and Tokyo; reversal alone fails, with the raw `passed: false` and separate authorized exception intact. There is **no post-coherency result**.

At `00:13:21.167401Z`, the operator sends SIGINT to the exact owned ladder child. The [stop receipt](evidence/rental-h100-20260905/native-fp8-followup/atlas-native-lat01/operator-budget-stop.json) reports about3 decode tokens/s per active request and projects that C16 warmup plus three repetitions would exceed the 1,200-second measurement cap. That progress observation is not C16 aggregate throughput. The ladder command exits **-2**, emits `KeyboardInterrupt`, and leaves **no `finished_utc`**. Only C1 exists in `rungs`; the whole run is explicitly incomplete. The wrapper's exit0 and `requests-complete` marker mean cleanup was reached, not that the interrupted measurement passed.

The owned server stop returns `stopped`. The immediate device query briefly retains its stopped PID as `[No data]`; the later pre-launch admission at `00:14:19Z` records a fresh empty GPU query. Both observations are preserved. [Later admission](evidence/rental-h100-20260905/native-fp8-followup/later-empty-admission/admission.json). A separate repaired-source run follows below; this interruption remains a distinct incomplete result.

## Repaired native profile: both latency rungs complete

Atlas source `5cde118469d5f623c3e26da3055dd9001b781fc2`, binary SHA256 `5dadfd4df7902d63a2bf03625c74cfb3d864f266c9f350d56d2ad7d5c5981470`, preserves the original FP8 weights and scales and the explicit BF16 head. The narrow repair selects existing batch GEMV for up to four native FFN rows, then the transposed/pipelined/base GEMM fallbacks. Two production dispatch tests fail before the change; afterward eight focused checks and **651 model tests pass, 14 ignored**. Workspace rustdoc and affected clippy exit0; workspace doctests execute zero tests with two ignored. The CPU author's full tree matches the executed source. [CPU and numerical acceptance](evidence/rental-h100-20260905/native-fp8-fixed-premeasurement/README.md).

The actual H100 numerical suite first rejects invalid K (exit1), then passes base/pipelined GEMM and batch/scalar comparisons under their existing numerical criteria. GEMM cosine is at least0.9995; batch4 M4 has zero maximum absolute difference, while batch16 M16 has0.125. This is not a claim that every numerical case is bit-identical. The distinct-nonce quality diagnostic passes one C1 control plus16 simultaneous requests, all complete HTTP200 with the exact integer and no leakage. Maximum observed client HTTP overlap is16; it does not prove four active GPU rows. [Seventeen raw request results](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/concurrent-quality/summary.json).

| Completed latency profile | C | Output tokens/s | TTFT p50 / p99, ms | TPOT p50, ms |
|---|---:|---:|---:|---:|
| Atlas native `lat02` | 1 | 39.336592 | 1,075.969 / 1,075.969 | 21.298292 |
| Atlas native `lat02` | 16 | 64.998445 | 26,260.628 / 51,356.432 | 52.838 |
| vLLM `lat01` | 1 | 76.928325 | 285.632 / 285.632 | 11.928163 |
| vLLM `lat01` | 16 | 790.506667 | 1,324.001 / 1,439.214 | 15.056308 |

The unchanged Atlas ladder runs from00:35:16 to00:39:54 UTC: three successful C1 requests and48 successful C16 requests, zero errors, all actual1,193 prompt /256 completion tokens and length finishes. Warmup is separate from the three timed repetitions. Means of per-repetition statistics are displayed, not pooled percentiles. The raw `prompt_tokens_per_req` field is a sorted set of observed counts, not a per-request array; aggregate prompt counts also agree with each concurrency. [Finished ladder](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/ladder.json).

Boot passes at24.455s to health plus0.203s for first-token service. Both pre- and post-coherency retain **passed:false**, with reversal the sole failed probe; separate policy receipts allow performance and explicitly deny certification. The measurement wrapper records all requested outputs complete at00:40:09.531902Z, the ladder exits0, and the exact owned server stops. The immediate occupancy query retains its stopped PID as `[No data]`; that is not an empty-device assertion. [Completion](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/measurement-complete.json), [post-quality](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/coherency-post.json), [separate policy](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/quality-policy-post.json), [stop](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02/stop.json).

The real `compare.py` process exits0 and reports **LOSS at both rungs**: Atlas/vLLM throughput ratios0.51 and0.08. C16 remains a large performance gap, with approximately26 seconds median first-token delay under Atlas's active-batch4 profile. Context, KV, sequence capacity and activation arithmetic remain different, so this is an observed profile comparison. The repaired native C1 improves from28.926 to39.337 tokens/s, chiefly through TTFT (3,409 to1,076ms); TPOT remains about21.3ms. The previous native C16 is missing, so its speedup is **NO-PAIR**. In the native-before/after output, legacy `Atlas`/`vLLM` column names mean new Atlas/old Atlas. [Actual A/B output](evidence/rental-h100-20260905/native-fp8-completed/comparisons/compare-benchmark.qwen38.atlas.native-lat02-vs-benchmark.qwen38.vllm.lat01.md), [native before/after](evidence/rental-h100-20260905/native-fp8-completed/comparisons/compare-benchmark.qwen38.atlas.native-lat02-vs-benchmark.qwen38.atlas.native-lat01.md).

Before trusting these comparisons, a derived copy with mismatched OSL is refused with exit2; original ladder hashes remain unchanged. Commands, exits, script hashes, full repetition series and stdout/stderr are captured. Comparator exit0 validates its workload/rung checks, not whole-session completeness, engine identity, matched precision or certified coherency. It also accepts the earlier incomplete run's C1, which remains labeled incomplete here. [Known-bad stderr](evidence/rental-h100-20260905/native-fp8-completed/comparisons/known-bad-osl.stderr), [derivation and commands](evidence/rental-h100-20260905/native-fp8-completed/comparisons/measurement-summary.json).

## Longer workload: both engine profiles complete

The separate vLLM `agent01` ladder requests nominal ISL4,096 / OSL512 and records actual4,593 /512 tokens per request, one warmup and three repetitions. It completes at00:20:46 UTC with zero errors across51 timed requests. Pre/post coherency again fail solely reversal, with the authorized policy preserved separately. Boot is88.583s plus0.256s first-token service; the owned process stops and the final occupancy query is empty.

| Agent workload profile | Output tokens/s | TTFT p50 / p99, ms | TPOT p50, ms |
|---|---:|---:|---:|
| vLLM C1 | 78.450530 | 387.335 / 387.335 | 12.011444 |
| vLLM C16 | 638.758198 | 3,457.176 / 4,675.695 | 18.225 |
| Atlas native C1 | 33.129225 | 3,194.166 / 3,194.166 | 23.989439 |
| Atlas native C16 | 55.120146 | 63,684.249 / 123,654.765 | 60.366346 |

These agent-workload rows are not compared to the1,024/256 latency shape. Atlas's fresh `native-agent01` session now completes all51 measured requests, actual4,593/512 tokens, zero errors and length finishes, from00:42:24 to00:53:21 UTC. It uses the same `5cde118` source, `5dadfd4d…` binary and native-FP8/BF16-head profile as `native-lat02`. Boot passes at21.352s plus0.200s first-token service. Both pre/post raw coherency gates stayfalse solely for reversal, and both separate policies pass. Measurement completion is00:53:36.587157Z; ladder and server wrapper exit0, with exact owned stop. The exported immediate query retains `[No data]`; the operator subsequently observes empty GPU at00:54 UTC. vLLM's captured Python hash is process ownership evidence, not immutable vLLM implementation identity. [Control ladder](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01/ladder.json), [commands and exits](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01/measurement-commands.json).

The actual same-shape agent comparator exits0 and reports **LOSS for both rungs**, Atlas/vLLM throughput ratios0.42 at C1 and0.09 at C16. Median C16 TTFT is63.7s for Atlas versus3.46s for vLLM under their recorded capacity/precision profiles. This remains a large queueing/service gap, not a certified comparison with matched engine settings. The same OSL-mismatch known-bad refusal is rerun before these fresh outputs. [Completed Atlas ladder](evidence/rental-h100-20260905/native-agent-completed/atlas-native-agent01/ladder.json), [genuine comparison](evidence/rental-h100-20260905/native-agent-completed/comparisons/compare-benchmark.qwen38.atlas.native-agent01-vs-benchmark.qwen38.vllm.agent01.md), [commands and exact statistics](evidence/rental-h100-20260905/native-agent-completed/comparisons/measurement-summary.json), [C1 provenance](evidence/rental-h100-20260905/native-agent-completed/atlas-native-agent01-c1.section10.json), [C16 provenance](evidence/rental-h100-20260905/native-agent-completed/atlas-native-agent01-c16.section10.json).

A remaining source finding concerns **BF16-head performance, not precision**. The actual loader says the head is kept BF16, and the final BF16 branch in `crates/spark-model/src/model/trait_impl/lm_head_batched.rs` executes the correct dense BF16 GEMM. That branch ignores the existing `dense_gemv_batchm_kernel` and the default-on helper in `decode_a2.rs`; the existing batched GEMV route is unavailable there. The native FFN representation repair remains intact. A narrow routing repair is in progress, with no speedup or runtime result claimed in this baseline. The prefill ledger attributes about16.94s of each63s latency C16 repetition to chronologically mapped prefill log intervals; these are host log intervals, not GPU event durations, and the remaining wall time is not automatically all LM-head time. [Captured ledger](evidence/rental-h100-20260905/native-agent-completed/bf16-head-performance/prefill-ledger.json). [Agent export and comparison manifest](evidence/rental-h100-20260905/native-agent-completed/MANIFEST.json).

The subsequent standalone `vllm bench serve` cross-check **fails with exit1** during random-dataset construction: `--random-input-len is too small` for the selected input range ratio1.0, whose minimum sampled length is0. No raw benchmark-result JSON or cross-check throughput is produced. This is separate from the completed frozen agent ladder; its `measurement-complete` receipt predates the cross-check. The original argv, stderr and failed Section10 record are retained. [Cross-check failure](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01/crosscheck/bench.stderr), [failed-cell provenance](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01-crosscheck.section10.json).

## Butter original FP8: successful load, one unchanged quality exception

Butter `dfc45a185fc36ee84a8351eef21ae1dfc56d527a`, binary SHA256 `da3e60000d55a2aaca67c04b09cd0400553444e45653d911ae828f06103cf606`, now validates and loads the full original block-FP8 MTP sidecar. The preceding floating-only validation failure remains in the earlier report. Iron is still composite `a4c897ba2e89db49df82b9d2f2691642d8f8b697` (PR299 plus PR315). The changes are published on [Butter PR594](https://github.com/waffuruai/butter/pull/594#issuecomment-5555724372).

Three real GPU numerical tests pass with exact FP32-bit oracles for tails, unequal block scales, tensor offsets, dense reduction and independent projection origins. First health200 appears after 10.084 s, with 32,279 MiB loaded device memory. The first HTTP request returns `Hello`, status200, with13 prompt /1 completion token. These are correctness-session timings; authorized CPU tests overlap, so they are not isolated performance measurements.

Determinism, tool schema, think-off,391 and Tokyo pass. The final reversal is `rotaregifer` rather than `rotaregirfer`, so the complete original coherency record stays **false / exit1**. The separate user exception authorizes only that reversal failure. Plain and tool SSE structure checks both pass, including final usage and terminal events; the typed call is `get_weather({"city":"Reykjavik","days":3})`. In this response, Butter emits its first tool delta together with completion, so its first-tool-delta time is not an internal first-generation-token measurement. [Coherency](evidence/rental-h100-20260905/native-fp8-followup/butter-native-correctness/coherency.json), [exception](evidence/rental-h100-20260905/native-fp8-followup/butter-native-correctness/coherency-exception.json), [numerical output](evidence/rental-h100-20260905/native-fp8-followup/butter-native-correctness/numerical.stdout), [tool stream](evidence/rental-h100-20260905/native-fp8-followup/butter-native-correctness/tool-stream/report.json).

The correctness session finishes at `00:05:12.790699Z`; owned SIGTERM shutdown and empty occupancy are recorded. No frozen Butter latency ladder is present in this snapshot. `serve-local` uses context8,192 and parallel1 with the exact snapshot path as its HTTP model identifier; further performance and concurrency validation are pending.

## Precision and Section10 provenance

The defensible alignment is the same original checkpoint weight quantization, with engine arithmetic disclosed. Atlas uses mixed W8A16 and dynamic W8A8 paths with BF16 outputs and its FP8/BF16 KV profile. Butter's initial CUDA-core operator uses F32 activations, scale multiplication, accumulation and output (W8A32); its mixed BF16 tensors remain original. vLLM selects dynamic block W8A8 and auto KV with BF16 model dtype. Context/capacity also differ. These sessions do not establish identical arithmetic, cache precision or a pure kernel-speed comparison.

Separate supplemental records provide the Section10 fields from actual retained evidence without fabricating campaign artifacts: [Atlas C1](evidence/rental-h100-20260905/native-fp8-followup/atlas-native-lat01-c1.section10.json), [Atlas interrupted C16](evidence/rental-h100-20260905/native-fp8-followup/atlas-native-lat01-c16.section10.json), [Butter correctness](evidence/rental-h100-20260905/native-fp8-followup/butter-native-correctness.section10.json). Each binds command/environment, source-build identity, executable hash, pinned snapshot, device/driver and `nvidia-smi -q` digest, workload and explicit completeness. Image digests are null for these native source-built processes. Butter's correctness-driver Git revision was not recorded; its actual driver, frozen oracle and stream-probe hashes are retained instead, with that gap explicit. No engine SHA is substituted for a harness identity. PTX/certification fields remain unproven where no applicable record was supplied.

[Raw exports and SHA256 manifest](evidence/rental-h100-20260905/native-fp8-followup/MANIFEST.json). Stop rule: preserve original false/incomplete records, stop owned processes, and require new gates for the next source or profile. No schema, threshold or frozen harness is changed by this documentation.

Completed-session Section10 supplements: [Atlas latency C1](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02-c1.section10.json), [Atlas latency C16](evidence/rental-h100-20260905/native-fp8-completed/atlas-native-lat02-c16.section10.json), [vLLM agent C1](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01-c1.section10.json), [vLLM agent C16](evidence/rental-h100-20260905/native-fp8-completed/vllm-agent01-c16.section10.json). These bind each completed rung to its actual argv/environment, engine/process distinctions, pinned snapshot, device digest, launch and measurement harness revisions, false raw gates and exception. [Completed raw files and manifest](evidence/rental-h100-20260905/native-fp8-completed/MANIFEST.json).
