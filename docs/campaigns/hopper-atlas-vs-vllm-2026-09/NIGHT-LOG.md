# Night log — 2026-09-05

Running status for the overnight split. Atlas side (this branch) is frozen on
`crates/`, `kernels/`, `Cargo.*` from tip `792579b` so the GB10 gate records
being measured on Spark 2 stay valid. Docs and `scripts/` may still move.

| time (UTC) | who | what |
|---|---|---|
| 03:50 | lead | Tip `792579b` pushed to fork; PR #895 body synced; fork PR #1 (Codex control-leg tooling) merged into the campaign branch; typos excludes verbatim evidence; LoC cap satisfied (lib.rs 433, inherited_targets.rs 373 + 164). |
| 03:55 | lead | Handed Codex the overnight plan: Step A GB10 rehearsal (vLLM + Atlas, Nano FP8, Spark 2, 70 GB cap), Step B the 10 REQUIRED perf-gate records at `792579b`, Step C the inferspark static-smem question. |
| 03:58 | lead | S7 in flight: `scripts/start-node-ep.sh` single-node N-GPU launcher + DEPLOYMENT.md section (scripts/docs only). Watch armed: 10-min poller + 2-hourly review pass. |
| 04:20 | lead | S7 landed: `scripts/start-node-ep.sh` (N ranks on one node, NCCL profiles default/debug/gb10-roce, dry-run, health poll, `--stop`) + `scripts/start_node_ep_test.sh` (13 assertions incl. 503→200 stub) + DEPLOYMENT.md section. Finding: `--check-kernels` must run single-rank (NCCL init precedes the audit). Pushed `0aeb32e`. Waiting on Codex Steps A/B. |
| 04:45 | lead | PR #895 rewritten to 12 thematic commits (code + receipts only); perf-path trees byte-identical to `792579b` (tagged `gate-tip-792579b`). Prose moved to issue #899 and this branch (`campaign/docs-hopper-2026-09`). Codex's vllm-control/** commits will be routed here from now on. Next: cell runner + vLLM control launcher + artifact schema (`bench/campaign/`), x86_64 binary CI + Dockerfiles, RUNBOOK. |
| 05:05 | lead | Landed on PR #895 (now 20 commits): `docker/{hopper,b200}/Dockerfile` + `datacenter-binaries.yml` (first CI run in flight on the PR), `bench/campaign/` cell kit (run_cell, vllm_control with 29 verified profiles, atlas_recipes, artifact schema + validator, 30-assertion suite), ladder `--enable-thinking` (think-on rows were unmeasurable before), warmup fixture. RUNBOOK.md added on this branch. Perf-path invariant vs `gate-tip-792579b` still holds (empty diff). |
| 05:40 | lead | Codex Step A (fork PR #2) folded. vLLM Nano on GB10 green except agent C=16 vacuity; **Atlas Nano FP8 degenerate output** (repeated primes) passed the determinism oracle and failed the tool-call gate. Hypothesis: FP8 checkpoint through nvfp4 bundle with missing FP8 KV scales (default 1.0). Asked Codex for Step A2 diagnosis (bf16 KV; fp8 KV + calibration; NVFP4 checkpoint) before Step B; Step B target = current tip (tree identical to 792579b). Lead to harden coherency_gate with loop detector + known-answer probes. |
| 06:15 | lead | Coherency gate hardened: repetition-loop detector (token-repeat >50%, distinct 3-grams <0.35, ≥8 consecutive identical segments) + bounded-answer truncation + the three known-answer probes; the recorded GB10 loop is a committed negative fixture that the OLD gate passed (exit 0) and the new gate fails (exit 1). `known_answer_ok` carried through assemble.py, cell_assemble.py, schema. Every FP8 Atlas recipe entry now has `--fp8-kv-calibration-tokens 256`. PR #895 at 24 commits, tip `294d289`; code pushes paused so the queued `Datacenter binaries` run can start (each push cancelled the previous queued run). |
| 07:05 | lead | Codex review on #895: 8 findings (5 P1), all reproduced red-first and fixed; each thread answered with its commit. The preflight thread-context bug was real on hardware (CUDA_ERROR_INVALID_CONTEXT on a spawned thread, GB10) → fixed by ordinal-addressed `cuDeviceGet`; gate tip moved to `gate-tip-5f827f1` (Codex told to measure at tip ≥ 5f827f1). Suites: campaign 43, node-ep 18, ptx-gate 7. PR at 30 commits, tip `0d342eb`. |
| 09:00 | lead | Codex review rounds 2–4 on #895 (signal finalizer, interrupted-create leaks on both engine paths, refused-launch ownership, engine_version attribution): all fixed red-first and answered per thread. Suites: campaign 65, node-ep 20, validator 26/26. PR at 35 commits, tip `cd820a1`; perf-path tree still equals `gate-tip-5f827f1`. CI still queued org-wide. Codex Step A2/B/C PR not yet opened. |

- 2026-09-05T11:29:58Z — PR #895 CI queue started draining: CLAAssistant, Certification state, LoC cap, block_on guard, GDN PINS, GitGuardian, classify-diff all pass at tip d3ef933. nvcc→PTX (gb10/hopper/b200), fmt, typos, cargo deny, Datacenter binaries still pending.
- 2026-09-05T12:18:45Z — `Build mdBook + rustdoc` failed on #895 (private intra-doc link in arch_preflight.rs). Reproduced with the CI command on Spark 1 CPU (worktree off atlas-hopper-gate-full, warm target), which also exposed a second broken link in ptx_set.rs the CI run had not reached. Both fixed (15e1b3d, b2c17cf), workspace rustdoc green locally, PR tip b2c17cf, new tag gate-tip-b2c17cf; Codex told on fork PR 2. Codex branch codex/gb10-prefill-shared-memory (Step C write-up, docs only) exists, no PR yet.
- 2026-09-05T12:53:52Z — Codex fork PR 3 (Step C gb10 static-smem write-up; A2 and B not started) routed: docs → 783fd19a here, trimmed HARDWARE.md note → c7db4dd on #895, summary on issue #899, PR closed. Codex found the gate ledger records only the first ptxas error per file (22 files vs 42 entries); fix underway red-first. Step D brief (real-GPU wrong-arch refusal, matrix dry-render, model pins) handed to Tom for Codex.

- 2026-09-05 — Resumed after the Mac reboot. Temporary local worktrees were lost; published code c4c1b56 is intact and unpublished patches were recovered from saved task logs into persistent owned workspaces. Fork PR4 was rebased onto the docs branch: 1,319 of 1,320 contributed blobs preserved exactly, with the canonical manifest reconciled; metadata JSON and local prose links validated. Historical Step D results remain tied to their original code and policy SHAs.

- 2026-09-05 — Restored and validated recipe/provenance/architecture changes; campaign77, renderer329/253, validator26, assembler8, launcher30, Docker17, PTX7 and strict Hopper27B181/181 pass. Reproduced MiniMax unmapped thinking labels and failed-audit normal launch before fixing both. Incorporated PR904's separate watchdog follow-up after red2→green runtime292 and rustdoc; final perf tag gate-tip-8d125b31 has no new GPU records.
- 2026-09-05T15:54:33Z — A2 stopped before model download when Spark2 acquired foreign computePID269913. Task-owned image and scratch exported/verified then removed; df56GiB initial,45GiB before cleanup,48GiB after. A2/B/D1 runtime oracles remain unobserved.
- 2026-09-05 — Restored first x86 Hopper artifact receipt after reboot; binary SHA61f45de1d1aa5c8124fcf2301e840c9d8ae6ed3b0a0a4848bbeba4c8f23fed27 matches bundled and BUILD-INFO hashes. No duplicate issue notification for that already reported milestone. Reviewed Claude's PR903/904 and preserved the separate RTX track.

- 2026-09-05 — Tom confirmed Vast.ai and $3.965/hour for both H100 SXM GPUs, 500 GB disk and a separate 500 GB volume. Added [eight-hour pilot plan](VAST-H100-8H-PLAN.md), cost/metadata receipts and six successful dry renders. Booking remains on hold: Vast container mode cannot execute the current nested-Docker vLLM path, and process launch provenance must be proven. Selected Qwen35 spec/think off as the first candidate pair; found a complete official Super TP2 cookbook profile for a separately frozen follow-on. No recipe change, model download, GPU job or rental was performed. The earlier 2–4-hour readiness estimate is superseded pending a tested execution bundle.

- 2026-09-05T16:38:59Z — Dispatched the Qwen3.6-35B Hopper x86 build on the verified TheTom/atlas fork at 8d125b31, [run 33978523017](https://github.com/TheTom/atlas/actions/runs/33978523017), with `hw=hopper`, `model=qwen3.6-35b-a3b`. Build is in progress; no artifact success claimed. Reran publication gates: campaign 77, launcher 30, Dockerfiles 17, renderers 329/253, validator 26, assembler 8, PTX CPU suite 7, formatting/typos/local shellcheck green. Spark 1 worktree removed after the CPU suite; no GPU work.

- 2026-09-05 — Tom replaced the rental storage selection with **300 GB instance disk and no volume**, at $3.789/hour for both H100 SXM GPUs. Updated the plan and machine-readable budget: eight-hour base $30.31, or $33.04 with 200 GB total traffic; $1.41 less than the prior allocation. All model transfers now use the instance disk, with download/verification time inside the rental window. Qwen plus Super is about 165.87 GB before runtime/caches; export results before destroying the instance. Removed volume staging/retention and fallback creation from the plan. Code and execution prerequisites are unchanged; no rental was made.
