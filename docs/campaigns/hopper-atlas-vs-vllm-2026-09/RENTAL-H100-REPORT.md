# Single-H100 rental: current results

Latest completed Atlas engine for both workloads: **`7c786cc`, original FP8 checkpoint with BF16 head**. Restoring the batched BF16 output-head route raises observed C16 throughput **13.6%, from 64.998 to 73.807 tokens/s**; C1 is effectively unchanged. The recorded vLLM profile reaches 790.507 tokens/s at C16, still about 10.7 times Atlas throughput. Atlas permits four active decode sequences and 128 slots; vLLM has a 512-sequence cap. Capacity, KV precision and activation arithmetic differ, so this is a comparison of the observed profiles, not matched engine settings.

These are **real single-H100 results**, not GB10 rehearsal or two-GPU data. The original raw coherency gates remain false solely for word reversal; the user's exception permits diagnostic performance work and does not certify it. Both head-fix workloads finish by 01:33 UTC; Butter finishes C1 at 01:38 UTC, the vLLM repeat and cross-check at 01:45 UTC, and a separate Atlas attribution probe follows. No benefit from profiling is claimed.

## Latest latency results

All three engines consume the same original `Qwen/Qwen3.8-27B-FP8` checkpoint, revision `017b9c7af6b5689d5dd426a76e0bc077eb5ca20a`, on one NVIDIA H100 80GB HBM3, driver 610.57.04. The frozen latency workload requests nominal 1,024/256 tokens and records actual 1,193/256 per request: one warmup, three timed repetitions, C1/C16, temperature 0, seed 42, think/spec off. Every timed request completes with length finish and zero errors.

| Engine/profile | C | Server capacity | Output tok/s | TTFT p50 / p99, ms | TPOT p50, ms |
|---|---:|---|---:|---:|---:|
| Atlas native `7c786cc` | 1 | Active 4; slots 128 | 39.349 | 1,074.195 / 1,074.195 | 21.298 |
| Atlas native `7c786cc` | 16 | Active 4; slots 128 | 73.807 | 23,476.673 / 45,680.003 | 45.449 |
| vLLM 0.28.0 `lat01` | 1 | Sequence cap 512 | 76.928 | 285.632 / 285.632 | 11.928 |
| vLLM 0.28.0 `lat01` | 16 | Sequence cap 512 | 790.507 | 1,324.001 / 1,439.214 | 15.056 |
| Butter original FP8 `dfc45a1`, W8A32 | 1 | Parallel 16; offered 1 | 5.723 | 35,282.265 / 35,282.265 | 36.898 |

Throughput and displayed latencies are arithmetic means of per-repetition values, not pooled percentiles. C1 has one request per repetition, so p50 and p99 coincide and do not establish a tail distribution. The frozen ladder driver SHA256 is `7a78f205e168a6ded92a6de270c46c59ea6e4dd18616c3927b98433128343fbb`. Atlas and vLLM enable prefix caching; frozen nonce restart, warmup, preceding gates and retained caches limit cold/warm interpretation. The tables retain the original vLLM `lat01` control; a fresh latency repeat is reported separately below. No independent agent-workload repeat was run.

Atlas `native-head-lat01` finishes at 01:21:14 UTC, with all requested outputs and post-policy complete at 01:21:29.598129Z. Boot passes at 23.372 s to health plus 0.199 s first-token service. Pre/post determinism, typed tools, no-think-leak, arithmetic 391 and Tokyo pass; reversal alone fails. Original gates stay false; separate policy receipts pass. The exact owned server stops and the wrapper exits 0. [Finished raw ladder](evidence/rental-h100-20260905/native-head-lat-completed/atlas-native-head-lat01/ladder.json), [post gate and exception](evidence/rental-h100-20260905/native-head-lat-completed/atlas-native-head-lat01/quality-policy-post.json).

The genuine comparator reports LOSS versus vLLM at both rungs. Against the previous `5cde118` native profile, the C16 ratio is 1.1355; captured argv excluding the executable and selected precision environment match. Its tiny C1 arithmetic WIN (about 0.03%) is not treated as a meaningful gain. A derived mismatched-OSL input refuses exit 2 before positive comparisons; raw ladder hashes remain unchanged. Comparator exit 0 is not certification of complete sessions, provenance, precision or coherency. [Actual before/after](evidence/rental-h100-20260905/native-head-lat-completed/comparisons/compare-benchmark.qwen38.atlas.native-head-lat01-vs-benchmark.qwen38.atlas.native-lat02.md), [actual A/B](evidence/rental-h100-20260905/native-head-lat-completed/comparisons/compare-benchmark.qwen38.atlas.native-head-lat01-vs-benchmark.qwen38.vllm.lat01.md).

## Control repeat and independent client cross-check

A fresh vLLM process runs the same latency ladder at 01:44:03–01:44:42 UTC, with all 51 timed requests completing at 1,193/256 tokens and zero errors. Pre/post raw gates retain reversal alone; both exception receipts pass. C1 repeats at **75.313 tokens/s**, down **2.1004%** from `lat01`; C16 repeats at **790.533 tokens/s**, up **0.00333%**. The unmodified comparator reports **LOSS / WIN**, respectively. Its exact-ratio `TIE` is demonstrated only by the identical-input selftest; it is not a tolerance rule for independent observations. Mismatched OSL refuses exit 2 before comparison. [Repeat ladder](evidence/rental-h100-20260905/vllm-final-aa-crosscheck/vllm-crosscheck02/ladder.json), [actual A/A output, both inputs vLLM](evidence/rental-h100-20260905/vllm-final-aa-crosscheck/comparisons/compare-benchmark.qwen38.vllm.crosscheck02-vs-benchmark.qwen38.vllm.lat01.md).

The separate `vllm bench serve` cell then completes **8/8 requests**, zero errors, exactly 1,024 input and 256 output tokens each: **76.687 output tokens/s**, TTFT p50/p99 **318.763 / 329.873 ms**, median TPOT **11.924 ms**. This uses random prompts through `/v1/completions`, `ignore_eos=true`, one readiness request and one warmup; it is a client cross-check, not the frozen essay/chat workload. Endpoint ownership passes before/after, followed by owned stop and empty GPU query. The raw result’s `max_concurrent_requests=2` counts inclusive one-second time buckets, despite configured concurrency 1; source inspection and a non-overlap counterexample explain it without changing the raw JSON. Random completion text contains think tags/repetitions and is not a chat coherency result. No additional post-coherency follows this client cell. [Concurrency metric audit](evidence/rental-h100-20260905/vllm-final-aa-crosscheck/client-preparation/CONCURRENCY-METRIC.md). The prior dataset-construction failure remains retained. [Exact command and result receipt](evidence/rental-h100-20260905/vllm-final-aa-crosscheck/vllm-crosscheck02/crosscheck/receipt.json), [raw vLLM JSON](evidence/rental-h100-20260905/vllm-final-aa-crosscheck/vllm-crosscheck02/crosscheck/vllm-bench-raw.json).

## Longer workload and Butter status

The agent workload is nominal 4,096/512 and actual 4,593/512 tokens, with the same warmup and three-repetition policy. Both engines complete all 51 timed requests with zero errors; the original pre/post gates retain the sole reversal failure and separate policy approval.

| Engine/profile | C | Server capacity | Output tok/s | TTFT p50 / p99, ms | TPOT p50, ms |
|---|---:|---|---:|---:|---:|
| Atlas native `7c786cc` | 1 | Active 4; slots 128 | 33.131 | 3,194.240 / 3,194.240 | 23.990 |
| Atlas native `7c786cc` | 16 | Active 4; slots 128 | 61.393 | 57,992.007 / 112,239.304 | 52.932 |
| vLLM 0.28.0 `agent01` | 1 | Sequence cap 512 | 78.451 | 387.335 / 387.335 | 12.011 |
| vLLM 0.28.0 `agent01` | 16 | Sequence cap 512 | 638.758 | 3,457.176 / 4,675.695 | 18.225 |

Atlas finishes at 01:32:51Z and final post-policy/measurement at 01:33:06.786677Z, followed by owned stop and wrapper exit 0. The head repair improves observed agent C16 throughput **11.4%**, from 55.120 to 61.393; C1 is effectively unchanged. vLLM remains about 10.4 times Atlas throughput at C16 under the recorded profiles. The actual comparator reports LOSS versus vLLM at both rungs. Earlier partial snapshots and the preceding `5cde118` baseline remain unchanged. [Finished agent ladder](evidence/rental-h100-20260905/native-head-agent-completed/atlas-native-head-agent01/ladder.json), [actual A/B](evidence/rental-h100-20260905/native-head-agent-completed/comparisons/compare-benchmark.qwen38.atlas.native-head-agent01-vs-benchmark.qwen38.vllm.agent01.md), [actual before/after](evidence/rental-h100-20260905/native-head-agent-completed/comparisons/compare-benchmark.qwen38.atlas.native-head-agent01-vs-benchmark.qwen38.atlas.native-agent01.md).

Butter now completes a full frozen C1 rung on the exact original checkpoint: three repetitions at **5.722727 tokens/s mean**, 0.162% rate spread, mean wall 44.73394 s, actual 1,193/256 tokens, zero errors and length finishes. Mean per-repetition TTFT p50/p99 is 35,282.265 ms and TPOT 36.897755 ms. Pre/post raw gates stay false solely for reversal; separate policy passes. Owned stop and empty GPU finish 01:38:39.595952Z, with minimum sampled free memory 17,603 MiB. Its `dfc45a1`/`da3e6000…` correctness baseline uses W8A32 CUDA-core compute, raw F32 KV, context 8,192 and server parallel 16 even though offered concurrency is 1. It preserves checkpoint weight quantization but differs from Atlas/vLLM arithmetic and capacity. [Finished raw ladder](evidence/rental-h100-20260905/butter-final-c1/ladder.json), [exact metrics](evidence/rental-h100-20260905/butter-final-c1/BUTTER-FP8-C1-FINAL-METRICS.json), [per-cell provenance](evidence/rental-h100-20260905/butter-final-c1/butter-final-c1.section10.json).

The preceding Butter C16 admission expires 180 s before the ladder, without captured OOM/CUDA error; its earlier C1 window retains two partial stdout records but no full rung/post-gate. Those failures remain unchanged. **No Butter C16 throughput is available.** Three numerical GPU oracles and short-request protocol checks established original-FP8 correctness before the completed performance run. [Prior attempts](RENTAL-H100-NATIVE-FP8.md#follow-on-preparation-after-the-completed-baseline).

## What changed and what is proven

- The Hopper MMQ capability repair stops exporting unsupported Blackwell entry points that previously resolved and then trapped with CUDA 719. The repaired Qwen3.8 model boots and serves. The earlier capacity refusal and trap remain documented in the [capacity history](RENTAL-H100-QWEN38-CAPACITY.md).
- The native FFN route now preserves the FP8 overlay for batched projections and selects existing batch GEMV/pipelined GEMM kernels. That lifted the earlier interrupted native profile to a complete 51-request latency run. CPU dispatch oracles, numerical criteria and the 17-request distinct-nonce quality check are retained; HTTP overlap 16 is not proof of four simultaneous GPU rows.
- `7c786cc` restores the existing BF16 batch-GEMV output-head route for 1–8 aligned rows, retaining scalar fallbacks and original precision. CPU red 2 pass / 1 fail precedes green 3 pass; full model 654 pass / 14 ignored, clippy/rustdoc exit 0, and doctests zero executed/two ignored. Nine H100 example comparisons are bit-identical to repeated scalar GEMV. Their N values 1,024/3,072/9,216 are example shapes, not the model's 248,077 active vocabulary rows or padded 248,320×5,120 checkpoint tensor, and not the old scalar-GEMM accumulation order. The complete model ladder is the separate performance observation. [CPU source/tree proof](evidence/rental-h100-20260905/head-fix-cpu-and-butter-admission/atlas-cpu/REPORT.md), [actual GPU output](evidence/rental-h100-20260905/native-head-lat-completed/gpu-numerical/numerical.stdout).
- Earlier Qwen3.6 tool fixes remove duplicate prompt preparation, account for grammar preparation in TTFT, and preserve exact masks while improving warm decode from about 56 to 169–179 tokens/s. One final serial cold first-tool observation is 3.379 s; cold cost remains substantial and no distribution is inferred. [Full grammar evidence](RENTAL-H100-GRAMMAR-FIX.md).

The latest published tooling head is `f08d4eafb3f56e2168bfc993a657191a4e424a1e`, adding only owned-launch support and tests for the explicit `ATLAS_MS_PROFILE` observability key. It does not change the compiled paths or measured binary. The separate diagnostic described below follows the frozen runs and supplies no throughput claim. The executed Atlas engine source for both workloads is `7c786cc50455dee52c11c3bf4097de945fbb8f6a`; binary SHA256 is `23efba747b5b309a1750789e1166055e9b4432e0511462da719e626cce01db64`. Actual argv requests native FP8, explicit BF16 head, context 24,576, FP8 KV with 256 calibration tokens and automatic BF16 layers, active batch 4, 128 slots and SLAI. vLLM logs dynamic W8A8 FP8 weights, BF16 model dtype, auto/BF16 KV, context 262,144 and 512 slots. Equal checkpoint bytes do not make arithmetic or capacity equal. The measured Atlas and Butter ELF files are archived outside Git with verified SHA256 receipts: [Atlas, 80,123,712 bytes](evidence/rental-h100-20260905/final-profile-and-archives/atlas-binary-archive.json) and [Butter, 60,723,664 bytes](evidence/rental-h100-20260905/final-profile-and-archives/butter-native-block-fp8-archive.json). The canonical `spark` path now has owned process evidence; native vLLM's immutable installed implementation identity remains unknown. Python hashes and native process image digests are not substituted for it. [C1 provenance](evidence/rental-h100-20260905/native-head-lat-completed/atlas-native-head-lat01-c1.section10.json), [C16 provenance](evidence/rental-h100-20260905/native-head-lat-completed/atlas-native-head-lat01-c16.section10.json).

## Where the remaining work is

The final `ATLAS_MS_PROFILE` probe observes **12 actual C4 steps** on the unchanged `7c786cc` binary and passes all 17 arithmetic requests. Median synchronized whole-step time is **38.1555 ms**: the 48 SSM transformer layers take **26.165 ms** (about 68.6%), the 16 attention layers **11.0285 ms** (28.9%), and final normalization plus head **0.951 ms** (2.5%). Both layer buckets include FFNs. There are three times as many SSM layers, so this does **not** identify recurrence as the bottleneck. The next focused measurement should separate native FP8 FFN work, GDN projections and recurrence. It should preserve this checkpoint/profile and then rerun uninstrumented C1/C16. [Raw phase output](evidence/rental-h100-20260905/final-profile-and-archives/atlas-msprofile01/profile-summary.json), [phase interpretation and limits](evidence/rental-h100-20260905/final-profile-and-archives/tooling-prep/ATTRIBUTION-FINAL.md).

The profiler synchronizes each whole layer and disables multi-sequence CUDA graphs; its times include CPU launch/synchronization overhead and are not exclusive GPU timings or a prediction of frozen throughput. Tooling admission is proven red on `7c786cc` before green on `f08d4ea`; all 16 Linux process tests pass in 6.204 seconds, and the compiled-path diff is empty. Diagnostic endpoint ownership and cleanup pass. [Linux output](evidence/rental-h100-20260905/final-profile-and-archives/linux-tooling/linux-green.stdout).

Historical default Atlas 51.208 tokens/s, its basename-related artifact NO-GO/null pin, the interrupted native 28.926 C1 run, original false gates and failed standalone vLLM random-data cross-check all remain unchanged below the current results in the [native companion history](RENTAL-H100-NATIVE-FP8.md). [Per-session provenance index](RENTAL-H100-PROVENANCE.md) and [current raw manifest](evidence/rental-h100-20260905/native-head-lat-completed/MANIFEST.json) retain exact engine/process, checkpoint, device, harness and completion boundaries. Final stopped-dataset verification passes at **01:55:49 UTC: 1,740 files, 14,577,630 bytes**, every size/SHA256 matching the rental results and download-proof trees. An earlier check found only the still-appending telemetry CSV; its failed receipt and exact owned collector stop remain retained. Final GPU, Spark and compiler queries are empty; disk reports 500 GB total, 117 GB used and 384 GB available. This proves the stated export scope, not weights, credentials or build trees. The user was asked to destroy Vast instance `49994640`; destruction/billing termination is not independently verified here. [Final hash proof](evidence/rental-h100-20260905/final-export-proof/final-export-verification.json), [final host state and scope](evidence/rental-h100-20260905/final-export-proof/MANIFEST.json).

## Resumed rental window after the completed baseline

The user resumed the rental with a bounded budget after the report above was published at `d1e8df8`. The preceding results and 01:55:49 export proof describe that completed baseline and remain unchanged. Initial targets reserved GPU work through 02:35 UTC and exports through 02:40 UTC, with a 02:45 UTC cutoff. Rebuilding the kernel and optional example consumed that reserve; the actual final session completes at 02:44:39 UTC as recorded below.

The resumed window completes an SSM subphase diagnostic, a native-FP8 attention output-projection dispatch repair, strict GPU numerical checks and real-model quality checks. An initial numerical mismatch exposes a shared accumulation-order defect, which is corrected and retested. No new frozen ladder runs; performance gains remain unmeasured. The evidence below records the source and binary identities, failed attempts, final checks and export proof separately from the preceding baseline.

### Resumed SSM detail observation: completed at 02:11 UTC

The unchanged measured `7c786cc`/`23efba74…` engine completes `detailprofile01` at 02:11:20 UTC. Only the three recorded profiler environment switches are added; the native checkpoint, BF16 head and serve argv are unchanged. Tooling source `1ed0334` has the same tree as its red-first CPU preparation and changes no compiled path. The actual Linux process suite passes all 16 tests in 6.939 seconds. All 17 arithmetic requests pass, followed by owned stop and wrapper exit 0. This diagnostic does not rerun the original coherency suite. [Fresh diagnostic and provenance](evidence/rental-h100-20260905/resumed-window/ssm-detail-completed/ssm-detail.section10.json).

The outer profiler records 12 actual C4 steps, one actual C3 padded to 4, and one C2 drain step. Its more heavily instrumented C4 median is 44.738 ms; comparison with the previous 38.1555 ms is **not a performance regression measurement**. The original inner padded-width-4 summary contains 624 layer invocations. Exact reassignment to each following outer step separates 576 invocations from twelve true C4 steps and 48 from the padded C3 step; each step has 48 complete phase and detail rows, with no trailing records. The values below use only actual C4. [Raw groups](evidence/rental-h100-20260905/resumed-window/ssm-detail-completed/diagnostic/ssm-subphase-summary.json), [exact reassignment and checks](evidence/rental-h100-20260905/resumed-window/ssm-detail-completed/ATTRIBUTION-BY-ACTUAL-BATCH.json).

| SSM region, actual C4 only | Median per layer invocation | Median of per-step sums across 48 SSM layers |
|---|---:|---:|
| Mixer, including norms and projections | 396 µs | 19.041 ms |
| Dense FFN plus residual | 281 µs | 13.536 ms |
| GDN recurrence, accumulated across four sequence calls | 98 µs | 4.6885 ms |
| Four recurrence subregions together | Not summed from separate medians | 10.421 ms |

The dense FFN and the distributed recurrence/projection work both warrant further investigation; these measurements do not establish GDN alone as the dominant kernel bottleneck. They are synchronized wall observations, not exclusive GPU times or predicted gains. Missing layer IDs, nested/reset timers and synchronization/logging overhead prevent stronger attribution. The recurrence return-tail label is a post-return tail, not a duplicate total. [Scope and source audit](evidence/rental-h100-20260905/resumed-window/ssm-detail-completed/ATTRIBUTION-FINAL.md).

### Attention output-projection repair: CPU proof

Source `6b76f3d83cd4eadc90b62100a87925f46d2a4c8e` reuses the existing block-FP8 batch4 kernel for contiguous attention output-projection rows in chunks of up to four. Original weight bytes/scales, scalar fallbacks and the later LoRA fold remain in place; no kernel arithmetic or numerical threshold changes. The known-bad production path emitted four scalar launches where one batch launch was expected (exit 101). Three focused tests then pass, followed by 657 model tests with 14 ignored, clippy/rustdoc exit 0 and workspace doctests exit 0 (zero runnable/two ignored). Tested agent source `ce75585` has identical compiled paths to the integrated source; differences are the documented tooling environment keys. [CPU receipt and scope](evidence/rental-h100-20260905/resumed-window/attention-oproj-cpu/CPU-IMPLEMENTATION-RECEIPT.json).

The report helper separately proved that the new latency label preserves previous same-workload comparisons. A known-bad quality-only marker was previously mislabeled as a pending ladder; explicit `measurement-intent.json` now separates that diagnostic. Actual request rows and command success are required to report its quality oracle passed; intent/cleanup markers alone cannot. Ten CPU fixtures pass, including false-row, failed-command and genuine incomplete-ladder counterexamples; original coherency and separate exception receipts remain distinct. The frozen ladder, comparator and schema are unchanged. [Red/green reporting receipt](evidence/rental-h100-20260905/resumed-window/summary-oproj-helper/receipt.json).

### Actual-shape GPU numerical check: initial failure retained

The `6b76f3d` numerical example finishes at 02:23:13 UTC with **exit 1**. The same oracle first refuses output-bit, guard and nonfinite mutations. Actual O-projection shape N=5,120 / K=6,144 is bit-identical at M2, but M4 differs in **three BF16 values**, printed maximum absolute error 0.000488281 and cosine 1.0. M5 is not reached. The strict bit-equivalence gate therefore fails; printed cosine 1.0 does not overrule it. No model-quality or frozen-latency session starts on this source. The failure and synthetic tensor scope remain explicit while the shared accumulation-order repair is evaluated separately. [Exact numerical output](evidence/rental-h100-20260905/resumed-window/attention-oproj-gpu-red/numerical.stdout), [failed oracle and identity](evidence/rental-h100-20260905/resumed-window/attention-oproj-gpu-red/NUMERICAL-FAILURE.json).

### Shared batch-kernel correction and follow-up source

Source inspection identifies a preexisting arithmetic-order difference in the shared batch4/batch16 template: it summed each pair of products before adding to the accumulator, while scalar GEMV added each product separately. Integrated source `97375dfc940be63cfac6c30195224b363ad42145` restores scalar FP32 accumulation order and extends the unchanged exact-output/finite/guard oracle to batch16 M8/M16 while preserving the original failing fixture bytes. Kernel CPU tests pass 75 with 7 ignored. These checks do not substitute for the corrected GPU run. [Correction and CPU identity](evidence/rental-h100-20260905/resumed-window/shared-kernel-fix-cpu/SOURCE-AND-CPU.json).

The corrected `spark` build completes with binary SHA256 `3200435f5b1e382e0df4b2c846acd0d061ffd2fa52bb4bbfdbed4ac94d6d1258`. Its build is a separate observation from numerical/model validation and any frozen performance run. [Exact build receipt](evidence/rental-h100-20260905/resumed-window/corrected-source-build/MANIFEST.json).

The first corrected-source numerical-example build hits its 300-second cap (build exit 124) after compiling 181 CUDA kernels and progressing through Rust crates. There is **no numerical execution or numerical verdict** from that attempt. It is retained separately from the earlier actual M4 mismatch. The unchanged source/flags resume against the warm target in a fresh evidence directory with a 90-second build cap; that is not a changed kernel configuration or a relaxed oracle. [Interrupted build](evidence/rental-h100-20260905/resumed-window/corrected-example-build-timeout/BUILD-INTERRUPTION.json).

The warm resume then exposes a Rust example compile error (exit 101): `KernelLaunch` was imported from `spark_runtime::gpu` instead of `spark_runtime::kernel_args`. This also occurs before numerical execution. Source `8882f088b3e1382456c72a7288c267b8dc6985de` fixes only the example imports relative to 97375df; it does not change the measured server/kernel binary. The paid-window lesson is to compile the exact optional GPU-example feature set before relying on default CPU tests: those successful tests did not compile this new optional example. [Exact compile failure](evidence/rental-h100-20260905/resumed-window/corrected-example-import-error/build.stderr).

### Corrected GPU numerical oracle: passed at 02:43 UTC

The corrected example completes at 02:43:02 UTC with exit 0. At the actual N=5,120 / K=6,144 O-projection shape, **M2/M4/M5 through batch4 chunks and M8/M16 through batch16 all match scalar BF16 output bytes exactly**, with zero maximum absolute difference, finite outputs and intact guards. Output-bit, guard and nonfinite mutants are rejected first by the same unchanged oracle. Example source 8882f08 and binary `3c9a84972a8c347a8625035a68f49c6ae6c6050622bedd1ef18b203b1a4f2663` are recorded separately from the corrected 97375df server binary. The original M4 failure and both build failures remain retained. This establishes the selected numerical cases, not new model throughput. [Raw exact-output proof](evidence/rental-h100-20260905/resumed-window/attention-oproj-gpu-green/numerical.stdout), [numerical identity and limits](evidence/rental-h100-20260905/resumed-window/attention-oproj-gpu-green/NUMERICAL-PASS.json).

### Corrected model quality: completed, no new throughput run

The fresh corrected server boots in 24.385 seconds plus 0.204 seconds for its first token. Server source 97375df/binary `3200435f…` and harness source 8882f08 are captured independently. All 17 distinct arithmetic requests pass, with observed HTTP-attempt overlap 16; the original full coherency suite passes determinism, typed tools, no-think-leak, 391 and Tokyo. **Reversal alone still fails, so original `passed:false` and command exit 1 are retained**; the separate user exception returns exit 0. Requests finish at 02:44:39.277229Z, followed by owned server stop and wrapper exit 0. The immediate stopped-PID `[No data]` query is retained rather than misreported as empty; fresh resumed-host proof is separate. [Original full gate](evidence/rental-h100-20260905/resumed-window/attention-oproj-quality/session/coherency.json), [separate policy](evidence/rental-h100-20260905/resumed-window/attention-oproj-quality/session/quality-policy.json), [complete session provenance](evidence/rental-h100-20260905/resumed-window/attention-oproj-quality/quality.section10.json).

**No new frozen latency or agent ladder ran in the resumed window.** The throughput tables remain observations of 7c786cc and must not be attributed to 97375df/8882f08. The follow-up proves the selected corrected kernel cases and real-model quality checks; its performance benefit remains unmeasured. The model session is explicitly quality-only, not a failed or incomplete benchmark.

### Resumed closeout and remaining measurement

Fresh host queries confirm no GPU tenant, no Spark process and no compiler; disk reports 500G total, 117G used and 384G available. GPU work ended at 02:44:39 UTC before the 02:45 cutoff. The corrected 97375df server ELF is archived outside Git (80,124,312 bytes, SHA256 `3200435f…`), and the resumed export subsequently verifies **2,047 files / 16,440,705 bytes** at 02:47:37.781754 UTC. The prior 1,740-file proof remains an unchanged snapshot of the first completed window. [Fresh host/export proof](evidence/rental-h100-20260905/resumed-window/final-resumed-proof/MANIFEST.json), [resumed provenance index](evidence/rental-h100-20260905/resumed-window/RESUMED-PROVENANCE.json). Provider destruction/billing termination is not independently verified in these records.

The next measurement is an uninstrumented same-profile C1/C16 ladder on the corrected server, after the same numerical and quality gates. Until then, the existing 73.807/61.393 Atlas C16 results belong to 7c786cc; neither the O-projection dispatch repair nor the shared arithmetic-order correction has a measured end-to-end throughput gain here.
