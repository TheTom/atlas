# Read-only audit of Claude's rental plan and kit

Inputs: the user's Obsidian rental plan and `README.md`, `dl_queue.sh`, `install_vllm.sh`, `preflight_node.sh`, `logit_diff.py` from `/Users/tom/dev/rental-kit`. [Source receipt](source-receipt.json) pins every file's SHA256. All six originals remained byte-identical after the checks. No kit/checkouts were edited; no package was installed; no GPU, remote host or external service was used by the reproductions. The shell reproductions run evidence-owned copies with only `/root` and `/etc/os-release` paths redirected and external commands stubbed. The logit tests exercise the copied production request/normalization/comparison path, replacing only its HTTP boundary. These are simulated failures, not results from the rented H100s.

**Do not use the kit's success sentinels as rental gates yet.** Four shell cases return success after required work fails or does not run, and two incomplete token streams receive `AGREED`. Keep the lead's pinned Qwen staging and tested process adapter as the execution path. The kit remains useful as a source of diagnostic ideas after the issues below are addressed by its owner.

| Priority | Finding and exact original lines | Oracle and observed red result |
|---|---|---|
| P0 | **A failed GPU occupancy query is reported as idle.** [preflight_node.sh:24](/Users/tom/dev/rental-kit/preflight_node.sh:24) assigns the line count even if `nvidia-smi --query-compute-apps` fails; `set -uo pipefail` does not stop the script without an explicit status check. Line25 treats zero output lines as no tenants. | Oracle: unknown occupancy must refuse GPU work. The stub returns43 and stderr `FIXTURE cannot query processes`; script prints `ok no foreign compute processes`, `PREFLIGHT: GO`, exit0. [Reproduction](process-query-failure-reports-idle.json). |
| P0 | **Install failures do not propagate.** [install_vllm.sh:9](/Users/tom/dev/rental-kit/install_vllm.sh:9) ignores the failing install pipeline, line10 still runs the Python probe, and line11's final `echo` becomes the shell status. | Oracle: package resolution/import failure must be nonzero. Stub installer exits17 and import probe19; observed `VLLM_INSTALL_EXIT=19 in 0s` but shell exit0. Only five install output lines are retained, losing the full failure context. [Reproduction](install-failure-exits-zero.json). |
| P0 | **Network failures still produce preflight GO.** [preflight_node.sh:39](/Users/tom/dev/rental-kit/preflight_node.sh:39) prints Python failure messages through `sed`; line44 neither exits nonzero nor informs the shell's `fail` variable, and line47 therefore reports GO. | Oracle: unreachable required model/source/toolkit endpoints must fail the advertised reachability gate. All three offline URL opens fail; all three `FAIL` lines are followed by `PREFLIGHT: GO`, exit0. [Reproduction](network-failure-reports-go.json). |
| P0 | **The queue can report complete with no model staged.** [dl_queue.sh:16](/Users/tom/dev/rental-kit/dl_queue.sh:16) continues after every disk deferral; line22 unconditionally prints `QUEUE_DONE`, which [README.md:37](/Users/tom/dev/rental-kit/README.md:37) treats as the gate. | Oracle: successful queue completion requires its requested checkpoints. With10G free, all three repositories are deferred, no HF command runs, then `QUEUE_DONE` and exit0. [Reproduction](deferred-queue-reports-done.json). |
| P1 | **Neither checkpoint nor runtime version is frozen.** [dl_queue.sh:18](/Users/tom/dev/rental-kit/dl_queue.sh:18) calls `hf download "$r"` without `--revision`. Lines13–15 mark/skip completion by repository name only. [install_vllm.sh:5](/Users/tom/dev/rental-kit/install_vllm.sh:5) fetches a mutable uv installer; line9 installs unversioned `vllm`. [README.md:85](/Users/tom/dev/rental-kit/README.md:85) then serves by HF ID. | Source-observed, not a downloaded-model experiment. A shared cache deduplicates storage but does not freeze a moving revision or prove that both engines loaded it. Printing package versions afterwards records an outcome but does not enforce the selected vLLM0.28.0 environment. Use the lead's pinned manifest, absolute snapshot, offline environment and recorded process interpreter/version. |
| P1 | **`--min-agree full` accepts a shorter matching prefix.** [logit_diff.py:168](/Users/tom/dev/rental-kit/logit_diff.py:168) compares only `min(lenA,lenB)`. Lines195–198 correctly label a length mismatch as divergence, but [line287](/Users/tom/dev/rental-kit/logit_diff.py:287) compares that index against the same shorter length through lines409–413. | Oracle: full agreement cannot pass with unequal streams. A=`[391]`, B=`[391, extra]` produces length mismatch=true, first_divergence=1 and pass=true; all four prompts end `AGREED`, exit0. [Reproduction](shorter-prefix-falsely-agrees.json). |
| P1 | **Empty legacy logprob streams pass.** [logit_diff.py:147](/Users/tom/dev/rental-kit/logit_diff.py:147) accepts `tokens: []`; the same line287 threshold becomes `0 >= 0`. | Oracle: no tokens provide no correctness oracle. Two responses with empty tokens/text and `finish_reason=length` yield n_compared=0, agreement0.0, pass=true and `AGREED`, exit0. [Reproduction](empty-legacy-streams-falsely-agree.json). |

The existing `logit_diff.py --selftest` still exits0 with `ALL 4 CASES BEHAVED AS EXPECTED`; its cases do not cover the two incomplete-stream failures. [Observed existing selftest](existing-selftest.log). Reproduce the seven additional observations with `python3 reproduce.py` in this evidence directory. It asserts the original files' hashes remain unchanged.

There are also concrete plan conflicts and diagnostic limits:

- **Wrong staging order for this rental.** [Plan lines24–30](</Users/tom/Documents/obsidian/Self Study/H100 Rental Day - Plan.md:24>) and [queue lines7–10](/Users/tom/dev/rental-kit/dl_queue.sh:7) fetch Nano NVFP4, Nano FP8 and Super FP8, but omit the selected first model Qwen3.6-35B FP8. The lead's current bounded first pair needs only the pinned 37.493GB Qwen repository. Nano NVFP4 diagnosis and Nano FP8 A/B are different checkpoint experiments and must be named separately. Do not spend the early rental window staging this old queue or silently mix its checkpoints with the selected pair.
- **Native installation is not the complete process adapter.** [Plan line28](</Users/tom/Documents/obsidian/Self Study/H100 Rental Day - Plan.md:28>) invokes the campaign runner after `install_vllm.sh`, but installing a Python venv alone does not establish the runner's direct process launch, interpreter path, ownership/cleanup, port/alias mapping, or provenance on a Vast container without nested Docker. Those are the lead's separate adapter work and gates. The kit's installer also does not export its venv's bin directory into later shells.
- **Unbounded retries and one-time disk checks.** [dl_queue.sh:14](/Users/tom/dev/rental-kit/dl_queue.sh:14) checks root free space once per repository, before an indefinite retry loop at line18. It does not re-check before/after each download attempt, retain a pinned byte/hash proof, or stop on permanent authentication/repository/disk failures. This contradicts the current bounded staging contract; retain the metadata-driven stager instead of layering the old queue over it.
- **Agreement is not semantic correctness.** [logit_diff.py:312](/Users/tom/dev/rental-kit/logit_diff.py:312) only prints expected-substring checks; they do not affect the verdict. Two identical `wrong` answers to every prompt produce `AGREED`, exit0. [Observed limitation](same-wrong-answer-is-agreement-only.json). This is consistent with a token-agreement diagnostic, but it cannot serve as the correctness gate described in [README.md:41](/Users/tom/dev/rental-kit/README.md:41). Keep the independent campaign coherency gate.
- **This compares generated sequences, not raw logits under a common forced prefix.** [Comparison lines173–188](/Users/tom/dev/rental-kit/logit_diff.py:173) use token strings and top-K logprobs returned from each engine's independently generated sequence. After the first differing token, the prefixes differ, so later deltas cannot isolate a numeric kernel defect. Token IDs/bytes and effective rendered prompt identity are not checked. [README.md:111](/Users/tom/dev/rental-kit/README.md:111) overstates chat-only divergence as proving a template bug: the raw path also changes the request/prefix and omits the chat thinking control ([logit_diff.py:106](/Users/tom/dev/rental-kit/logit_diff.py:106)). Treat that pattern as a lead for investigation, not an attribution gate.
- **The document still mixes older booking assumptions.** [Plan lines16–32](</Users/tom/Documents/obsidian/Self Study/H100 Rental Day - Plan.md:16>) describe a four-hour one-GPU shakedown, while the active run is the user's two-H100, 300GB, no-volume booking with the lead coordinating the available window. Use the current lead runbook for timing and destruction decisions; this audit does not initiate or end an instance.

Minimal owner follow-up: make each required probe and install explicitly propagate its result and retain full stderr; give a deferred/incomplete queue a nonzero status; pin runtime/checkpoint identity or use the lead's already prepared stager; reject empty/unequal streams for full agreement; label token comparison as a diagnostic and preserve the independent coherency gate. No patch was applied here. Bootstrap/build/sync/fault/profiling script implementations were outside this bounded audit and have not been certified by reading the README.

Stopping rule met: the specified files were read, concrete failure modes were reproduced offline, exact lines and raw evidence were saved, and the execution inputs stayed unchanged.
