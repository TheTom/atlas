# Claude rental-plan check

The plan's inventory checkbox was still accurate: review thread https://github.com/Avarok-Cybersecurity/atlas/pull/895#discussion_r3941259972 and follow-up https://github.com/Avarok-Cybersecurity/atlas/pull/895#discussion_r3941291995 concern the atlas-plugin gate inventory, not the separately updated atlas-kernels inherited-target suite. Published PR head8d125b31 and local4168042 both omitted the two Hopper Qwen27B entries in crates/atlas-plugin/src/gate/taxon_tests.rs. The independent Claude report was correct.

Fresh CPU red on a clean Spark1 worktree at8d125b31: the actual inventory test exits101,0passed/1failed. Its tree walk lists hopper/qwen3.6-27b/nvfp4 and hopper/qwen3.8-27b/nvfp4, while the expected list omits them. The fix adds exactly those two sorted entries and corrects the comment to seven Hopper targets. Commit fee26d73d24dc04ddb88bee8690459e0007df8a7. Full cargo test -p atlas-plugin is green:932 passed,0failed,0ignored across the crate and integration suites. Workspace cargo doc --workspace --no-deps exits0; formatting/typos pass. Source hash and raw command/stdout/stderr/exit receipts are retained. Because this is under crates/, the lead must create a new perf-tree tag after incorporating it.

The process audit environment gap was independently observed in the current working tree: cell_process.sh invoked the raw audit argv directly, whereas the server used process-env.json through the safe launch manager. A renderer check with RUST_LOG=error and no HF_HUB_OFFLINE produced server RUST_LOG=info, HF_HUB_OFFLINE=1 and SPT_NOENV=1. The raw audit inherited error/unset/unset instead, and could inherit other ambient variables excluded by safe launch. The audit must use the same safe environment construction as serve, while retaining the audit flag. See the captured source files, process-render-observed.json and audit-serve-environment-difference.json; the parent owns that fix.

No Obsidian plan edits, GitHub writes, RTX branch inspection or GPU work occurred. Owned inventory worktree removed after verification; shared warm checkout files and cargo target were retained. Before/after df and tested source identity are in the adjacent receipts.
