# Parallel cold grammar preparation — CPU diagnostic

Local commit `03116200bea718b792e585a2bbf04be41ddd6ee0`, based on `f66a262`. No GitHub writes or H100 GPU requests were made. This is Spark1 CPU evidence, not Hopper performance or a scored A/B result.

The compiler's existing `max_threads` now bounds scoped workers for the already-selected adaptive masks. The server permits at most four workers, capped by available CPUs; the caller participates and every worker joins before return. Spawn failure drains the same work with fewer workers. The 512-mask threshold, cost ranking, selected-state count, mask generator, sampler and grammar acceptance rules are unchanged. A short cache lookup filters already-cached selected states, so warm requests schedule zero worker jobs while still returning the original selected count. The shared Rayon sampling pool is not used.

The concurrency regression first failed with “selected masks are still computed serially” (exit 101). The follow-up warm-state regression first failed with “warm masks would still spawn worker jobs” (exit 101). Both pass after their fixes. All 826 xgrammar unit tests pass. Full supported `cargo test --locked -p spark-server` passes: 2,454 passed, no failures, 19 ignored; six ignored tests require live GPU models and one newly ignored test is the explicit offline native-tokenizer diagnostic. Existing xgrammar integration entries return early without their tokenizer path, so their two green entries are not claimed as live-tokenizer validation. Workspace rustdoc, formatting, typos and diff checks passed. Every edited Rust source file is at most 500 lines.

The native diagnostic uses the existing pinned Qwen3.6-35B-A3B-FP8 tokenizer, SHA-256 `5f9e4d4901a92b997e463c1f46055088b6cca5ca61a6522d1b9f64c4bb81cb42`, transferred from the already-downloaded H100 checkpoint. There were no Hugging Face or package downloads. Vocabulary size is 248320. The real server native get_weather compiler exported both auto and required grammars. An initial unoptimized server-path diagnostic passed six repetitions of full adaptive-mask equality, next-token mask equality, valid native continuation, malformed-function rejection and rollback equality (im_end stop only). The final optimized replay uses both live stop IDs `[248046, 248044]` and repeats those oracles for three fresh serial/parallel pairs per mode. The permanent explicit CPU test has been aligned to both stop IDs; the final replay is the optimized measurement, while the initial server-path run is retained separately.

| Mode | Serial cold median | Four workers cold median | Selected masks | Warm call before/after |
|---|---:|---:|---:|---:|
| Auto | 2194.670 ms | 1342.347 ms | 123 | 170.351 / 49.326 µs |
| Required | 2188.479 ms | 1754.847 ms | 123 | 142.585 / 45.373 µs |

Both cold paths start with fresh per-grammar and cross-grammar caches. All 123 selected masks are present before sampling; full adaptive masks compare equal. CPU scheduling was not pinned, and the first auto serial sample was faster than later samples; these small diagnostic samples do not predict the exact H100 host speedup. See `native-release-final.stdout` for all repetitions and `warm-overhead-before/after.stdout` for 100-call warm measurements.

Warm grammar fill is the larger recurring finding: final optimized means are 6.770 ms/content token (auto) and 6.785 ms (required), while accept_token averages 0.89–0.96 µs. Four positions account for nearly all fill time: positions 13 and 14 (newline and “Re” entering city), and 28 and 29 (newline and “3” entering days), approximately 67–68 ms each. Most other fills cost 2–70 µs. The 40-token content replay excludes the generation stop token. Per-position values are in `optimized-final-summary.json`; mask application, B1 and sampler copying are excluded from these fill timers. This finding was handed to the warm-path owner; no matcher changes are included here.

Reproduce with the committed explicit CPU test and `QWEN_TOKENIZER_JSON` pointing to the existing pinned file, setting `QWEN_GRAMMAR_EXPORT_DIR` to an owned scratch directory. The test writes native-true.ebnf, native-false.ebnf and output-ids.json. Place the tokenizer there as qwen-tokenizer.json; `cargo run --offline --release -p xgrammar --example native_prewarm_profile -- DIR` performs the optimized replay, and adding `--warm-only` measures cached calls. Exact command/environment receipts accompany each log.

All 1,985 tracked crate/Cargo/toolchain files matched SHA-256 between the local commit and the transplanted remote worktree (`source-verification.json`). Cleanup removed only the owned Spark1 worktree and its copied tokenizer/generated fixtures; the production checkout and shared warm target were preserved. Before/after df is in `cleanup.json`. The stopping rule is satisfied: exactness passed, an optimized cold improvement was observed, warm thread churn was eliminated, and the full CPU package/rustdoc gates passed. A fresh H100 build/retest and refreshed tree-based perf evidence remain the lead's deployment step.
