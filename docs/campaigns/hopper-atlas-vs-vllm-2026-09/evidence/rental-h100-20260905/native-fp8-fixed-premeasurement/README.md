# Fixed native FP8: acceptance before the second ladder

This snapshot captures completed checks at Atlas `5cde118469d5f623c3e26da3055dd9001b781fc2`, binary SHA256 `5dadfd4df7902d63a2bf03625c74cfb3d864f266c9f350d56d2ad7d5c5981470`. The second frozen latency ladder was running at00:34:35 UTC; no throughput or full-run completion is claimed here.

The change selects existing block-scaled batch GEMV for up to four native FFN rows, then the existing transposed/pipelined/base GEMM fallbacks. It retains original weight/scales and BF16 activation/output; no kernel or checkpoint quantization changes. The two production dispatch tests fail before the change and eight focused tests pass after it. Full model tests pass651 with14 ignored; workspace rustdoc and affected clippy exit0. Workspace doctests execute zero tests with two ignored. The CPU author's full tree matches the executed parent source. [CPU report](cpu/REPORT.md), [receipt](cpu/receipt.json).

The real H100 example checks pass for pipelined M5, pipelined M129, base M129 and batch/scalar equivalence. The invalid-K case exits1 first. These preserve the examples' numerical criteria: GEMM cosine≥0.9995; batch4 M4 reports zero maximum absolute difference, while batch16 M16 reports0.125. Do not call the whole numerical matrix bit-identical. Commands, outputs, exits and test-binary hashes are in [the manifest](MANIFEST.json).

A separate quality probe then completes one C1 control and16 distinct nonce arithmetic requests, all HTTP200 with complete bodies, exact expected integer content, stop completion and no reasoning/tool leakage. The client records16 overlapping HTTP attempts. This proves concurrent client requests, not four simultaneous GPU rows. All17 request JSONs, raw bodies, response headers and result records are retained in [the quality directory](concurrent-quality/summary.json). The unchanged standalone probe has nine known-bad oracle refusals before its two valid controls; a local HTTP test also proves overlap and bounded trickling-response cleanup.

The immutable launch prelude retains the actual canonical `spark` path, executable hash, pinned model, environment including nativeFP8, BF16-head argv, boot/pre-coherency, exception and device snapshot. Frozen ladder output and still-growing serve logs are intentionally absent from this acceptance-only snapshot. [Machine-readable acceptance](ACCEPTANCE.json). All source-built image/certification limitations remain; no scoring gate was overridden.
